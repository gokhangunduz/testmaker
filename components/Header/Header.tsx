"use client";

import { ReactElement } from "react";

export default function Header(): ReactElement {
  return (
    <header className="w-full h-16 sticky top-0 bg-white border-b border-zinc-300">
      Header
    </header>
  );
}
